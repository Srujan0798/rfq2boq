__pycache__/
*.py[cod]
.coverage
.pytest_cache/
.mypy_cache/
.ruff_cache/
.DS_Store
data/jobs/
*.log

# Model files (too large for GitHub)
# NOTE: only commit models/ENTRIES.md as an inventory; weights stay local.
# Past incident: a 2.8 GB duplicate model (models/ner_model/) accidentally
# committed and ate disk. Keep models/ gitignored entirely.
models/
data/real_rfqs/raw/
data/real_rfqs/extracted/
data/real_rfqs/templates/
results/demo/
resources/knowledge_base/

# Attic — archived out-of-scope code (preserved for reference but huge)
# Tracked in git but NOT loaded by tests/lint/type-check (excluded in pyproject)
# attic/  # uncomment if you want to fully exclude (loses history)

# DSR PDFs — too large to commit, download manually
data/rates/dsr_2023/raw_pdfs/
.venv311/
*.pyc
.codex/

# Purged synthetic data (re-purge task)
data/synthetic/


# synthetic corpus (purged/re-archived to attic/ per reorg cleanup)
data/synthetic_archive/

.venv-lora/
data/samples/

# Session handoff files (temporary, not for repo)
SESSION_HANDOFF*.md

# Purged data archives (large, not for git)
attic/data_purged_*/

# Annotation pipeline working files (auto-generated, not for git)
data/incoming/*
!data/incoming/.gitkeep
data/annotations/draft/*
!data/annotations/draft/.gitkeep
data/annotations/drafts/*.draft.json
!data/annotations/drafts/PRIORITY_QUEUE.json
data/annotations/drafts/.queue_progress.json
data/annotations/verified/*
!data/annotations/verified/.gitkeep
data/annotations/real_specs_silver.json
data/real_rfqs/INTAKE_MANIFEST.csv

# Generated evaluation artifacts (regenerated by test/eval scripts)
results/eval_honest.json
results/eval_honest_rows.json
results/final_model_eval.json
results/generalization_smoke.json
results/product_eval.json
results/product_validation.json
results/IMPROVED_PIPELINE_VALIDATION.json
session_*.zip

# Allow real incoming RFQs (not synthetic)
# DISABLED for completion: incoming/ is a working inbox; canonical copies live
# in data/specifications/ and data/real_rfqs/raw/. Re-enable only if you intend
# to commit a small, curated set of incoming RFQs.
#!data/incoming/*.xlsx
#!data/incoming/*.pdf
.venv/
results/eval_honest_v2.json
results/eval_honest_baseline_current_pipeline.json
results/eval_honest_rows_after_z2.json
FINAL_COMPLETION_SUMMARY.md
data/specifications/
"Specification 2.rar"
Specifications.rar
results/FIDELITY_REPORT.md
tasks/FINAL_PLAN.md

# Local databases (agent session state, never project data)
*.sqlite3
*.sqlite

# Generated fidelity artifacts (reproduced by scripts)
results/fidelity_audit_*.txt
results/fidelity_measure_run.txt
results/current_fidelity_*.txt
results/fidelity_audit_summary.txt

# Mined gazetteer (generated from client specs)
data/ontology/insulation_gazetteer_mined.json

# Session protocol logs (superseded by kleenhand.md)
tasks/ETERNAL_PROTOCOL.md

# KleenHand session cleanup artifacts
.session_exports/
.session_handoffs/
.session_merged/
.grok_handoffs/

# Batch extraction / CLI agent generated artifacts
output/
cli_agents/
logs/

# Ontology backups
data/ontology/backup/

# Running CLI agent training outputs (commit only when finished + validated)
models/rfq2boq-ner-lora-cli/

# In-progress CLI agent annotation workspace (commit when validated)
data/annotations/cli_training/
results/ui_dropin/
.dispatch_prompts/
results/annotation_wave1/verify_*.txt

# Local salvage dumps from Desktop chaos repo (not part of product)
archive/desktop_repo_salvage/

venv/
