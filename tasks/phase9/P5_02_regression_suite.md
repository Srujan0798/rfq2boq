# TASK P5_02: Turn the regression scaffolding into a real, enabled safety net — Agent-P5-2

## 1. GOAL
Enable the Tier-1/Tier-2 regression assertions for real — corpus-exact locks on verified documents and combination-invariance checks — so any future change (or rogue process) that degrades a verified result fails CI loudly and permanently.

## 2. CONTEXT
Files to read FIRST (in order):
- `tests/regression/test_corpus_exact.py` + `test_combinations.py` — the scaffolding built 2026-07-05 (4 real passes, 6 honest skips awaiting "genuinely verified docs" + "stable core") — both conditions NOW hold
- `results/fidelity/summary.json` — the verified per-doc state to lock
- `tasks/phase9/03_VERIFICATION_GATE.md` Gate 2 — these tests are part of every gate
- Tier definitions: Tier 1 = corpus regression (verified docs stay exact), Tier 2 = combination invariance, Tier 3 = held-out honest F1 (P4_02's job, NOT a pytest)

Current state:
- Skips were honest ("0 genuinely-verified docs yet; core extraction unstable"). After P1–P3, both preconditions are met. These files are FROZEN — your changes go through orchestrator re-pin (this task is one of the sanctioned frozen-file tasks).

## 3. DELIVERABLES
- [ ] `tests/regression/test_corpus_exact.py` — enabled: for every doc with verdict PASS in the accepted fidelity summary: run pipeline → FidelityAuditor → assert verdict PASS and captured-count exact; expectations loaded from a new locked `tests/regression/expectations.json` (doc_id → row_count, source_truth sha, generated by a script, human-reviewable)
- [ ] `tests/regression/test_combinations.py` — enabled invariance checks: (a) same doc via CLI path vs UI entry path → identical rows; (b) doc processed alone vs within a batch → identical; (c) re-run determinism (twice → identical); (d) unit-normalizer idempotence on real outputs; (e) dimension-class vs unit coherence flags don't change row counts
- [ ] `scripts/gen_regression_expectations.py` — regenerates expectations.json ONLY from an accepted `results/fidelity/summary.json` + prints a diff for orchestrator review (never silently overwrites)
- [ ] Runtime budget: the regression suite completes <15 min (mark the huge-PDF docs `slow` tier if needed, but at least 20 docs in the default tier)
- [ ] `Makefile` — `make regression` target; wired into `make verify`

## 4. STEPS
1. Read context; confirm the fidelity summary you're locking is the orchestrator-ACCEPTED one (ledger row reference in your report).
2. Build expectations generator + expectations.json; review the content yourself line-by-line against `results/fidelity/` (you are locking the project's proof — typos here are poison).
3. Enable Tier 1; run; every failure is REAL info — investigate each (test bug vs product bug vs stale expectation) and report; do not weaken an assertion to pass (Rule 5 — if a product bug surfaces, it goes to the orchestrator for a fix task, and THIS task waits).
4. Enable Tier 2 invariances; same discipline.
5. Timing pass; Makefile; commit; hand the frozen-file diff to the orchestrator for re-pin.

## 5. VERIFICATION
```bash
cd /Users/srujansai/rfq2boq-phase9
make regression                                        # EXPECT: all enabled tests pass, <15 min, 0 skips without written reasons
python3 -m pytest tests/regression/ -v 2>&1 | tail -20
python3 scripts/gen_regression_expectations.py --check # EXPECT: "expectations match accepted summary"
python3 -m pytest tests/unit tests/integration -q && make lint && make typecheck
```

## 6. ACCEPTANCE CRITERIA
- [ ] Every PASS-verdict doc locked exact; no doc silently omitted (count in expectations == PASS count in accepted summary)
- [ ] All 5 invariance families enabled with ≥1 real doc each
- [ ] Remaining skips: zero, or each carries a reason + a ledger-acknowledged owner sign-off
- [ ] Suite is deterministic (3 consecutive runs identical)
- [ ] `make verify` includes regression

## 7. CONSTRAINTS
- Rule 5 inverted onto you: you are EDITING frozen files legitimately — the orchestrator reviews your full diff and re-pins; nothing else in the frozen set moves
- No assertion may be weaker than the accepted fidelity state (exact counts, not ranges)
- Standing constraints: `CLAUDE.md` §7

## 8. DEPENDENCIES
- **Blocked by:** P3_04 (stable extraction stack), P1_03 (10/10 sacred to lock), P5_01 (UI-path invariance needs the single entry point)
- **Blocks:** P5_03, P5_04
- **Parallel-safe with:** P4_02
- **Shared files:** `tests/regression/**` (frozen), `Makefile`

## 9. GOTCHAS
- Batch-vs-alone invariance historically breaks via shared mutable state (ontology caches, normalizer singletons) — if (b) fails, the bug is usually a module-level cache; report it, don't retry-until-green.
- Determinism killers: set iteration + float formatting in exports — compare canonical JSON (sorted keys, rounded floats) rather than raw strings.
- UI-path test must not launch a server — call the shared entry function directly.
- If 05_zydus is PASS by then (D5 done), it gets locked like the rest; if D5 is still open, it appears in expectations as EXCLUDED with the reason string — visible, not forgotten.
